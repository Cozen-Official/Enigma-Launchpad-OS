title: Enigma OS Documentation
tagline: Fully-configurable in-world control surface for VRChat
description: Documentation for Enigma OS — material swaps, screen shaders, skyboxes, AudioLink, presets, faders, whitelists, and more, configured entirely from the Unity inspector.
show_downloads: false

remote_theme: pages-themes/midnight@v0.2.0
plugins:
  - jekyll-remote-theme
  - jekyll-relative-links

relative_links:
  enabled: true
  collections: true

include:
  - index.md
